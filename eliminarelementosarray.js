let numArray = [1,2,3,4,5]

function newNum(){
  numArray.push(6,7)
}

newNum()


//Creamos el array
let array = [1,2,3,4,5]
console.log(array)

// Aplicamos .shift()
let shiftArray = array.shift()

//Revisamos. El output debe de ser [2,3,4,5]
console.log(array)

// --- POP ---

//Creamos el array
let array = [1,2,3,4,5]
console.log(array)

// Aplicamos .shift()
let shiftArray = array.pop()

//Revisamos. El output debe de ser [1,2,3,4]
console.log(array)
Footer
© 2023 GitHub, Inc.
Footer navigation
Terms

// ¿Qué método debo utilizar para eliminar el último elemento de un array?

.pop()

//¿Qué hace el método `forEach()`?
Ejecuta la función indicada una vez por cada elemento del array.

// ¿Qué hace el método `find()`?}
RETORNA EL PRIMER ELEMENTO DE UN ARRAY QUE CUMPLUE CON UNA CONDICIUON DEFINIA EN UN NUEVO ARRAY

// ¿Qué hace el método `some()`?
Retorna "true"o “false” si hay elementos en un array que cumplan con la condición indicada.

// ¿Qué hace el método `filter()`?
Crea un nuevo array con los elementos del array que se pasó como parámetro y que cumplan con la condición definida.

//¿Qué hace el método `map()`?
crea un nuevo array con los resultados de la llamada a la funcion indicada aplicados a cada uno de sus elementos